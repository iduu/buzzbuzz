$("#comment_form").dialog({
  autoOpen: true,
  modal: true,
  width: 480,
  title: 'Reply to',
  position: ['center', 200],
  open: function() {
    disable_scroll()
    $("#comment_form").html("<%= escape_javascript(render('form')) %>")
  },
  close: enable_scroll
});