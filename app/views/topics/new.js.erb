$("#post_form").dialog({
  autoOpen: true,
  modal: true,
  width: 400,
  title: 'New buzz',
  open: function() {
    disable_scroll()
    $("#post_form").html("<%= escape_javascript(render('form')) %>")
  },
  close: enable_scroll
});