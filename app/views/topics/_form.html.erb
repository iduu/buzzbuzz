<%= form_for(@topic) do |f| %>
  <% if @topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@topic.errors.count, "error") %> prohibited this topic from being saved:</h2>

      <ul>
      <% @topic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <p class="clearfix"><%= f.label :title %>:<br />
  <%= f.text_field :title, :class => :post_input %></p>
  
  <p class="clearfix"><%= f.label :url %>:<br />
  <%= f.url_field :url, :class => :post_input %></p>
  
  <p class="clearfix"><%= f.label :text %>:<br />
  <%= f.text_area :text, :class => :post_input, :rows => 10 %></p>
  
  <div class="actions clearfix">
    <%= f.submit :class => :btn_submit %>
  </div>
<% end %>
