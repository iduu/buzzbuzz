$("<%= escape_javascript(render(:partial => @topics)) %>").hide().appendTo('#feed_list').fadeIn(2000);

if (<%= @topics.total_page %> > <%= @topics.current_page %>) {
  checkScroll();
} else {
  $('#loading').hide()
}
